apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-secret
  namespace: banco-srjm
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: vault-banco
    kind: ClusterSecretStore
  target:
    name: postgres-secret
    creationPolicy: Orphan
    deletionPolicy: Retain
  dataFrom:
  - extract:
      key: banco-srjm/postgres-secret
